name: global_state
description: Provides a GlobalState model for managing multiple counters.
version: 0.0.1
environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  scoped_model: ^2.0.0

flutter:
  uses-material-design: true
